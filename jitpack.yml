jdk:
  - openjdk17

before_install:
  - chmod +x gradlew

# 核心：通过环境变量指定 SDK 路径，并跳过那个报错的 get-android-jar 任务
install:
  - export ANDROID_HOME=$ANDROID_SDK_ROOT
  - JITPACK=true ./gradlew publishToMavenLocal -Pgroup=com.github.khaos -x :coding:get-android-jar:assemble -x test