jdk:
  - openjdk17

before_install:
  - chmod +x gradlew
  # 强制安装 Shadow 报错要求的 SDK 版本
  - yes | sdkmanager "platforms;android-33"

install:
  # 保持原样，这行逻辑没问题
  - JITPACK=true ./gradlew publishToMavenLocal -Pgroup=com.github.khaos -x test